Drop official company logo files here, named by project slug.
The dock + mini-dock will use them automatically; until a file exists,
a conceptual icon shows instead.

Expected files (named by project slug; .png or .svg):
  intuit.png
  capital-one-data.png
  capital-one-auto-refinance.png
  eureka-surveys.png
  archive.png

The dock tries .svg first, then .png, then .jpg.
